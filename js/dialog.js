function  open_dialog(){
        
 $('#dialog').dialog({
  modal: true,
  position: [0,0],
  resizable: true,
  maxHeight: 500,
  maxWidth : 500,
  minHeight: 150,
  minWidth : 150,
  height: 'auto',
  width : 600
     }); 

 }
